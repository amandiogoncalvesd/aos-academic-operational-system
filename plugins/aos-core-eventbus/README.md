# aos-core-eventbus

Plugin AOS ainda não implementado. Para activar, criar `manifest.json` + `src/plugin.py` com `register(ctx)`
(ver `plugins/aos-domain-sis` como referência). O loader ignora directórios sem manifest.
